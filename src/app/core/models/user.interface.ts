export interface User{
    username?: string,
    password: string,
    email?: string,
    email_confirmation?: string,
    first_name?: string,
    last_name?: string,
    password_confirmation?: string, 
}